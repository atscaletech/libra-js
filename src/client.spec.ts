describe('Client', (): void => {
  it('Query should be work.', (): void => {
    expect(true).toBeTruthy();
  });

  it('Query multi should be work.', (): void => {
    expect(true).toBeTruthy();
  });
});
